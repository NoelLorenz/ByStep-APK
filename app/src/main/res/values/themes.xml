<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ByStep" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ByStep" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <style name="CustomActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="android:displayOptions">showHome|showTitle</item>
        <item name="android:icon">@drawable/logo_with_name</item>
        <item name="displayOptions">showHome|showTitle</item>
        <item name="icon">@drawable/logo_with_border</item>
    </style>



</resources>